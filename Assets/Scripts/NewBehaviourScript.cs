using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;



public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    /*

    private void CheckScoreForHacking()
    {
        int totalScore = GetTotalScore();
        Text hackStatusText = GameObject.FindGameObjectWithTag("HackStatusText").GetComponent<Text>();

        if (totalScore >= 500)
        {
            hackStatusText.text = "HACKED BY YOU";
        }
        else
        {
            hackStatusText.text = "you has been hacked";
        }
    }
    */
}
